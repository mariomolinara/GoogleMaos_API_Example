package it.unicas.spring.googleapi.demo.config;

/**
 * @deprecated Replaced by {@link FleetConfiguration} and {@link BusLineConfig}.
 *             This class is retained only as a placeholder to avoid import errors
 *             during migration and will be removed in a future version.
 */
@Deprecated
public class RouteConfiguration {
    // No longer a Spring bean. Use FleetConfiguration instead.
}
